# M1 Acceptance Report

Status: **PASS — STOP after AC-1 / AC-2**

Date: 2026-08-14  
Workspace: `C:\Users\lenovo\Desktop\dshtui`  
Shared upstream checkout: `D:\deepseek-harness` (read-only)

## Delivered M1 slice

- `dsh-tui-app` bundle manifest and Cordis patch
- isolated `tui` profile installation through the existing `dsh plugin` command
- responsive Ink alternate-screen shell with Chafa-generated colored DeepSeek whale
- minimal pure event fold for committed `user/message` and `assistant/message` text
- Commander-owned TUI flags: `--resume`, `--theme`, `--color`
- deterministic unit tests and Windows ConPTY acceptance harness

M2 features are intentionally absent: streaming chunk reconciliation, Markdown, tool trees, diffs, subagents, and full unknown-event presentation.

## AC-1 — startup and terminal restoration

Machine report: `.m1-results/ac1.json` (generated by `node scripts/ac1.mjs`).

| Check | Result |
|---|---|
| `dsh plugin --profile tui add <workspace>` | exit 0; bundle tuple is `[@deepseek-ai/dsh-base, dsh-tui-app]` |
| `dsh --profile tui --help` | exit 0; all three TUI flags present |
| ConPTY geometry | 120x40 |
| shell rendered | `DEEPSEEK / HARNESS` and `TRANSCRIPT` observed |
| colored whale | truecolor DeepSeek-blue sequence observed |
| controlled exit | `q`, exit 0, no timeout |
| alternate screen | enter 1, leave 1 |

## AC-2 — zero kernel modification

Machine report: `.m1-results/ac2.json` (generated by `node scripts/ac2.mjs`).

The implementation root is structurally outside `D:\deepseek-harness` and exposes its own `./cordis.patch.yml`. A content snapshot covering the shared repository status and scoped binary diff was identical before and after the real install/help/PTY acceptance run.

The shared checkout already contained another benchmark participant's modifications before this work. Therefore its global diff is not empty; `ac2.json` records those pre-existing paths verbatim instead of attributing them to this project. No shared file was written, staged, reverted, or reformatted.

## Supporting checks

- `npm run check`: pass
- `npm run test:m1`: 3 files, 6 tests passed
- `npm audit --omit=dev`: 0 vulnerabilities
- `npm pack --dry-run`: 20 files; no Chafa executable or source PNG in package

## Visual critique

The signature spend is concentrated in the whale and two blue/cyan accents; the rest stays as a quiet, continuous work surface. At 120x40 the whale/title lockup reads clearly without competing with the transcript. At compact dimensions the whale is omitted, which preserves the transcript and footer rather than crushing the mark. The M1 frame is intentionally sparse: adding cards or permanent navigation before their data planes exist would create false affordances.
