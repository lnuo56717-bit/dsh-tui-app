# M2 Acceptance Report

Status: **PASS — STOP after AC-3 / AC-6 / AC-8**

Date: 2026-08-14  
Workspace: `C:\Users\lenovo\Desktop\dshtui`  
Shared upstream checkout: `D:\deepseek-harness` (read-only)

## Delivered M2 slice

- pure, serializable fold for the locked 44-event vocabulary and all seven stream chunk variants
- listener-first snapshot/live handoff with sequence de-duplication and gap re-snapshot
- streaming-to-final assistant reconciliation, including absent, empty, and populated `sourceEventSeqs`
- terminal Markdown presentation with non-executing HTML fallback
- grapheme-safe CJK/full-width/emoji wrapping and cursor-cell measurement
- nested tool lifecycle tree, Code Mode dispatch children, orphan results, and colored unified diffs
- workflow/subagent tree, runtime activity rows, scroll controls, and unknown-event raw placeholders
- real Windows ConPTY acceptance driver using a test-only profile overlay

M3 features were not entered: there is no composer, approval answerer, question form, session switcher, or resume implementation in this changeset.

## AC-3 — end to end

Machine report: `.m2-results/ac3.json` (generated by `node scripts/ac3.mjs`).

| Check | Result |
|---|---|
| command | real `dsh --profile tui`, isolated profile overlay |
| ConPTY geometry | 120×40 |
| profile installation | exit 0, using the existing `dsh` launcher |
| transcript | streaming increment, tool node, unified diff, and final message observed |
| disk result | exact UTF-8 content `深海工具写入成功，whale 🐋\n` |
| disk SHA-256 | `7f873451dd16b0ede175006100567c60a530258220ae798f8647aa378c3c6513` |
| controlled exit | exit 0, no timeout |
| terminal restoration | alternate-screen enter/leave balanced |
| shared checkout | snapshot unchanged |

The event producer lives only in `tests/fixtures/ac3-driver.mjs` and is inserted into the isolated acceptance profile. It appends real `Session` events over time and writes the fixture file; product UI code never fabricates acceptance events or bypasses the fold.

## AC-6 — CJK and display cells

- Mixed Chinese/English text, full-width punctuation, emoji, combining marks, headings, lists, tables, and inline Markdown are wrapped by grapheme and checked against terminal display width.
- The 120×40 PTY run observes Chinese content in the tool diff and final transcript without truncating the task marker.
- `cursorCell` is checked with `A中文，🐋éZ`, and clipping is proven to preserve the original grapheme sequence without replacement characters.

The task book schedules the actual input composer for M3. M2 therefore validates the shared cursor-column primitive that the composer will consume; the interactive input path must be repeated at the M3/M4 gates rather than being falsely claimed here.

## AC-8 — deterministic fold

- 250 seeded fast-check runs compare full replay with arbitrary incremental partitions.
- Re-injecting an already folded sequence is an identity no-op.
- Unknown future event types produce one raw node and do not crash.
- A sequence gap is never guessed across; the controller re-snapshots.
- A 5,000-event streaming regression preserves order, content length, and final sequence.

## Supporting checks

- `npm run check`: pass
- `npm test`: 8 files, 21 tests passed
- `npm run test:ac:m2`: 5 M2 files, 15 tests passed; AC-3 passed
- `npm run test:ac`: AC-1 and AC-2 regression passed
- `npm audit --omit=dev`: 0 vulnerabilities
- `npm pack --dry-run`: 29 files; no Chafa executable, source avatar, test fixture, or acceptance output included
- all direct `@deepseek-ai/dsh-*` dependencies remain exactly `0.1.0-rc.6`

## Visual critique

The whale remains the only decorative asset and disappears as soon as transcript content needs the space. Blue/cyan identifies structure, green/red identifies diff semantics, and muted text carries provenance without turning every node into a card. Tool and workflow trees share one compact grammar, while Markdown is allowed to establish hierarchy inside messages. This keeps a 120×40 terminal readable and recognizably DeepSeek without competing with the work being performed.
