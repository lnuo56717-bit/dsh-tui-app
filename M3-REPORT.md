# M3 Acceptance Report

Status: **PASS — STOP after AC-4 / AC-5**

Date: 2026-08-14  
Workspace: `C:\Users\lenovo\Desktop\dshtui`  
Shared upstream checkout: `D:\deepseek-harness` (read-only)

## Delivered M3 slice

- grapheme-safe single-line/multiline composer with movement, deletion chords, history, follow-up, and explicit steer
- fuzzy command sonar combining separate dsh and TUI descriptors, including explicit collision rows
- local help/keys/new/resume/session-info/rename/theme/model/workflow commands and guarded danger preset alias
- scoped terminal approval answerer with allow-once, reject, cancellation, and preset escalation
- structured user-question provider with plan-review intent, option navigation, multi-select, and free text
- persisted session picker plus command-line `--resume`, both using `agents.resume`
- teardown ordering that settles blocking interactions, unmounts Ink, restores the terminal, then disposes the owned handle

M4 features were not entered: projection-rich status details, final theme/capability polish, exhaustive compact-terminal QA, and completed user documentation remain outside this changeset.

## AC-4 — approval

Machine report: `.m3-results/ac4.json` (generated by `node scripts/ac4.mjs`).

Both cases ran through a real `dsh --profile tui` process in a 120×40 Windows ConPTY using an isolated test-only profile overlay.

| Check | `y` path | `n` path |
|---|---|---|
| approval card visible | pass | pass |
| service outcome | `allowed-once` | `rejected` |
| durable `approval/asked` | present | present |
| matching `approval/decided` | `allowed-once` | `rejected` |
| fixture action | exact file written | file absent |
| exit / terminal restoration | pass | pass |

The test driver opens a valid turn, records a real tool call, and invokes the public approval service. Product code supplies the scoped answerer; it does not synthesize either audit event. The allow fixture contains exactly `approval allowed once\n`. The reject path never creates its target.

## AC-5 — durable resume and follow-up

Machine report: `.m3-results/ac5.json` (generated by `node scripts/ac5.mjs`).

1. A first real TUI process creates a persisted session containing balanced user/assistant history and exits cleanly.
2. A second process starts with `dsh --profile tui --resume <same-id>`.
3. The runtime reports `agent/session-start.source = resume`; the same session id and full whale-history text are observed.
4. The PTY types `继续追问：请确认历史完整` into the actual composer and presses Enter.
5. The active resumed agent accepts an identified user-source message with that exact text through its inbox.

The persisted log had 9 events when the fixture published its id and 11 when resumed because the title service legitimately appended two later events. Resume therefore checks the stored watermark is not regressed and verifies the exact history content, rather than incorrectly demanding equality with an earlier live cut.

## Interaction and CJK evidence

- Editor tests cover Chinese, emoji, combining marks, cursor-cell calculation, backspace/delete, delete-word, and delete-to-edge operations without splitting graphemes.
- Controller tests cover real message shapes, follow-up, steer, approval resolution, structured questions, command-name collisions, startup resume, and in-process session switching.
- Approval and plan-review cards have terminal snapshots proving their action vocabulary and width stay within 120 columns.
- Blocking request aborts and teardown fail closed; Esc only parks focus and never invents a decision.

## Supporting checks

- `npm run check`: pass
- `npm test`: 11 files, 27 tests passed
- `npm run test:ac:m3`: 3 M3 files, 6 tests passed; AC-4 and AC-5 passed
- AC-1 / AC-2 / AC-3 regression: pass
- `npm audit --omit=dev`: 0 vulnerabilities
- all direct `@deepseek-ai/dsh-*` dependencies remain exactly `0.1.0-rc.6`
- shared upstream snapshot remained unchanged in every PTY acceptance run

## Visual critique

M3 spends its new visual complexity on one structural idea: the bottom composer is a quiet “sonar line” beneath the transcript. Blocking cards use double rules because they genuinely interrupt progress; command and session discovery use single-rule temporary soundings. No permanent sidebar or dashboard was added. The whale remains the only decorative mark, while state continues to rely on glyphs and language in addition to color. At 24 rows, secondary header copy is removed before transcript or composer space is sacrificed.
